import { PagePlaceholder } from '../components/layout/PagePlaceholder'
export function SettingsPage() { return <PagePlaceholder title="Settings" description="Configure the dashboard without exposing secrets to the client." /> }
