import { PagePlaceholder } from '../components/layout/PagePlaceholder'
export function InsightsPage() { return <PagePlaceholder title="Insights" description="See the operational signals that matter most." /> }
